fullnameOverride: outbox-publisher-service

namespace:
  name: microgo-dev

global:
  imagePullPolicy: IfNotPresent
  imagePullSecrets: []
  # Image used by wait-for init containers; override to mirror into a private registry.
  busyboxImage: busybox:1.36

component:
  name: outbox-publisher-service
  fullnameOverride: outbox-publisher-service
  replicaCount: 1
  waitFor:
    - { name: config, host: centralized-config, port: 8080 }
  containerPort: 8086
  image:
    repository: registry.digitalocean.com/hendafarhani-microgo-registry/microgo-outbox-publisher-service
    tag: latest
  service:
    enabled: true
    type: ClusterIP
    port: 8086
    targetPort: 8086
  configMap:
    enabled: true
    data:
      SPRING_CLOUD_CONFIG_URI: http://centralized-config:8080
      SPRING_CLOUD_CONFIG_NAME: outbox-publisher-service
      SPRING_CONFIG_IMPORT: optional:configserver:http://centralized-config:8080
  secret:
    enabled: false
    stringData: {}
  env:
    - name: SERVER_PORT
      value: "8086"
    - name: PORT
      value: "8086"
  readinessProbe:
    tcpSocket:
      port: 8086
    initialDelaySeconds: 20
    periodSeconds: 10
  livenessProbe:
    tcpSocket:
      port: 8086
    initialDelaySeconds: 40
    periodSeconds: 15
  resources: {}
